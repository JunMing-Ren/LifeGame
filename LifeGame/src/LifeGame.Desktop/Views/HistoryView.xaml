<UserControl x:Class="LifeGame.Desktop.Views.HistoryView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="{StaticResource PrimaryBrush}">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,20">
            <TextBlock Text="历史记录" FontSize="28" FontWeight="Bold" 
                       Foreground="{StaticResource TextBrush}"/>
            <TextBlock Text="{Binding TotalCount, StringFormat=' ({0}局)'}" 
                       Foreground="{StaticResource TextSecondaryBrush}" FontSize="16" 
                       VerticalAlignment="Center"/>
        </StackPanel>

        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,10">
            <TextBlock Text="已完成: " Foreground="{StaticResource TextSecondaryBrush}" 
                       VerticalAlignment="Center"/>
            <TextBlock Text="{Binding CompletedCount}" Foreground="{StaticResource AccentBrush}" 
                       VerticalAlignment="Center"/>
        </StackPanel>

        <ListBox Grid.Row="2" ItemsSource="{Binding Records}" 
                 Background="{StaticResource SecondaryBrush}" 
                 Foreground="{StaticResource TextBrush}"
                 BorderThickness="0">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <Border Background="{StaticResource PrimaryBrush}" 
                            CornerRadius="5" Padding="15" Margin="0,5">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <StackPanel Grid.Column="0">
                                <TextBlock Text="{Binding GameName}" FontSize="16" FontWeight="Bold"/>
                                <TextBlock Foreground="{StaticResource TextSecondaryBrush}" FontSize="12">
                                    <Run Text="{Binding CreatedTime, StringFormat='{}{0:yyyy-MM-dd HH:mm}'}"/>
                                    <Run Text=" | "/>
                                    <Run Text="{Binding EndingTitle}"/>
                                </TextBlock>
                                <TextBlock Text="{Binding TagList, Converter={StaticResource TagsConverter}}" 
                                           Foreground="{StaticResource AccentBrush}" FontSize="11" Margin="0,5,0,0"/>
                            </StackPanel>
                            <TextBlock Grid.Column="1" Text="{Binding IsCompleted, Converter={StaticResource CompletionConverter}}" 
                                       VerticalAlignment="Center" Margin="10,0"/>
                            <StackPanel Grid.Column="2" Orientation="Horizontal">
                                <Button Content="继续" Style="{StaticResource OptionButtonStyle}" 
                                        Width="60" Margin="5,0"
                                        Command="{Binding DataContext.ContinueGameCommand, RelativeSource={RelativeSource AncestorType=ListBox}}"
                                        CommandParameter="{Binding}"/>
                                <Button Content="删除" Style="{StaticResource OptionButtonStyle}" 
                                        Width="60" Margin="5,0"
                                        Command="{Binding DataContext.DeleteRecordCommand, RelativeSource={RelativeSource AncestorType=ListBox}}"
                                        CommandParameter="{Binding}"/>
                            </StackPanel>
                        </Grid>
                    </Border>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>

        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,20,0,0">
            <Button Content="刷新" Style="{StaticResource OptionButtonStyle}" 
                    Width="120" Command="{Binding LoadRecordsCommand}"/>
            <Button Content="返回" Style="{StaticResource OptionButtonStyle}" 
                    Width="120" Command="{Binding BackCommand}"/>
        </StackPanel>
    </Grid>
</UserControl>
